package com.keklol

/**
 * Reverts (reverses) the given string.
 *
 * @param input the string to revert
 * @return the reversed string
 */
fun revertx(input: String): String {
    return input.reversed()
}
